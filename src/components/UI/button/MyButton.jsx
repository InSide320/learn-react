import React from 'react';
import classesStyle from "./MyButton.module.css"

const MyButton = (props) => {
  return (
    <button className={classesStyle.myBtn}></button>
  );
};

export default MyButton;
